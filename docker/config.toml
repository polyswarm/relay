[relay]
    anchor_frequency = 100
    confirmations = 20
    consul = "http://consul:8500"
    community = "gamma"

[relay.homechain]
    wsuri = "ws://homechain:8546"
    free = false
    interval = 300

[relay.sidechain]
    wsuri = "ws://sidechain:8546"
    free = true
    interval = 30

[logging]
    format = "raw"
